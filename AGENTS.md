# 项目工程规范

> 本文件是游奕——多 Agent 值守（哨兵）中控项目开发必须遵守的准则。

## 项目概览

「游亦」是一个常驻个人电脑的"值守+遥控"中控。自动发现并接入你电脑上的多个 AI 应用，通过 Hook 机制实时感知任务状态；微信一个渠道搞定一切——收通知、发指令、和 Agent 对话，全部原样透传。
项目是一个跨端的 Electron 桌面应用，包括主进程（Node.js，常驻后台），渲染进程（Dashboard UI：任务卡片 / 首次引导 / 设置），使用SQLite · better-sqlite3 本地存储。

## 目录说明

```
docs/     项目开发相关的文档
  ├── references/  外部依赖组件/API 等参考文档
  ├── needs/       用户原始需求记录
  ├── specs/       需求规格描述文档
  └── design/      设计文档
```

## 全局规则约束

- **改动前先读**：动某个模块前，先读该模块对应的设计文档 + 相关源码，再动手。不要凭记忆猜测字段名、指标口径、路由路径。
- **不过度设计**：只解决当前任务。不加投机性抽象、不加用不到的错误处理与兼容层。
- **变更记录留底**: 每次变更的方案和影响评估结果，都应在代码完成后，写入 `changelogs/{YYYY-MM-DD-变更内容的一句话描述}.md` 中。
- **确保文档与实现一致性**：每次变更方案被确认之后代码实现之前，将对应的设计文档`design/`及需求规格文档`specs/`进行更新，避免出现文档与实现不一致的情况

## 绝对禁止事项

- ❌ 跳过测试直接提交
- ❌ 引入未经确认的新依赖
